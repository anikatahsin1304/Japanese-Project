{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rimraf styles && sass -w --color scss:styles",
    "build": "rimraf styles && sass --no-source-map --color scss:styles"
  },
  "dependencies": {
    "rimraf": "^5.0.5",
    "sass": "^1.69.5"
  },
  "engines": {
    "npm": ">=8.0.0 <9.0.0",
    "node": ">=18.13.0 <19.0.0",
    "yarn": ">=1.22 <2.0.0"
  }
}
